# AGENT.md

MabiKumaWeb 에서 에이전트가 따르는 작업 규칙.

## 커밋과 푸시

**논리 단위로 작업이 완료되면 대화가 끝난 후 커밋하고 푸시한다.**

- "논리 단위"는 그 자체로 말이 되는 변경 하나다. 기능 하나, 버그 하나, 리팩터링 하나.
  파일 개수나 줄 수가 아니라 "이 커밋만 떼어 놓아도 설명이 되는가"로 가른다.
- 대화 중간에 끊어서 올리지 않는다. 작업이 끝나고 검증까지 통과한 뒤에 올린다.
- 올리기 전에 반드시 통과시킨다: `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`.
  하나라도 실패하면 올리지 않고 먼저 고친다.
- 기본 브랜치는 `main` 이고, `main` 에 푸시하면 `deploy.yml` 이 돌아 mabi.spkuma.com 이
  바로 재배포된다. 배포가 딸려 온다는 것을 알고 올린다.
- 검증이 통과하지 못했거나 작업이 중간에 멈췄으면 올리지 않고, 무엇이 남았는지 말한다.

## 커밋 메시지

- 제목은 영어 한 줄, 본문은 한국어. 기존 이력과 같은 형식을 쓴다.
- 본문에는 "무엇을 했는지"가 아니라 **"왜 그렇게 했는지"** 를 적는다. 무엇을 했는지는 diff 가 말한다.
- 화면에 보이는 문자열과 마찬가지로 em-dash(`—`) 를 쓰지 않는다. 일반 하이픈을 쓴다.
- 데이터를 **어느 사이트에서** 가져왔는지, **누구의** 데이터나 프로젝트인지는 커밋 메시지(제목과 본문)에
  적지 않는다. "게임 클라이언트 데이터", "공개된 곳" 처럼 무엇인지만 적는다.
- 데이터를 긁어왔다, 복사했다, 옮겨 왔다는 언급도 커밋 메시지와 코드 주석에 남기지 않는다.
  데이터를 더하는 커밋은 그 데이터로 무엇을 할 수 있게 됐는지만 적는다.

## UI 작업

화면, 컴포넌트, 레이아웃, 색, 상태, 다크 모드를 건드리기 전에
[.claude/skills/mabikuma-ui/SKILL.md](.claude/skills/mabikuma-ui/SKILL.md) 를 먼저 읽는다.
antd 하나만 쓰고, 색과 반경은 `src/app/theme.ts` 에서만 나온다.

## 데이터와 정직함

- 화면에 보이는 수치는 API 응답에서 오거나, 목업이라고 코드에 표시되거나, 둘 중 하나다.
- 불러온 건수와 화면에 보이는 건수가 다르면 그 사실을 화면에 적는다. 숨기지 않는다.
- 게임 데이터는 평균 10분 지연되고, 이 사이트는 개인이 만든 비공식 도구다. 두 고지를 지운 채 배포하지 않는다.

## 의존성

새 라이브러리는 사용자 승인 없이 추가하지 않는다. 필요하면 설치 명령과 이유를 먼저 제시한다.
